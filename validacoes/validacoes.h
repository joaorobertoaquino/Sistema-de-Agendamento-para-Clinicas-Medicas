int ehLetra(char c); // Função que verifica se um caractere é uma letra
int validarNome(char *nome); // Função que valida se uma string contém apenas letras
int validar_cpf(const char *cpf); //Verifica se o cpf é válido
int validar_data(char *data);
int validar_celular(char *celular);
int validar_email(const char *email);
int validar_endereco(char *endereco);
int validar_data_cadastro(const char *data_cadastro);
int validar_hora(const char *hora);
int validarEspecializacao(char *especializacao);
int validar_CRM(const char *CRM);
int validar_tempo(char *duracao);
int validar_ID(int id);
int validarNome_procedimento(char *nome); 